<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--<Button Text="Stop" Margin="20, 10, 20, 10" Command="{Binding StopCommand}" IsVisible="True" IsEnabled="{Binding IsRunning}" />
        <Button Text="Start" Margin="20, 10, 20, 10" Command="{Binding StartCommand}" IsVisible="True" IsEnabled="{Binding IsRunning, Converter={StaticResource InvertedBoolConverter}}" />-->
        <Label Grid.Row="0" Text="{Binding DeviceInfoText}" />
    </Grid>

</ContentPage>
